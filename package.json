{
  "name": "portfolio-root",
  "version": "1.0.0",
  "scripts": {
    "build:frontend": "npm run build --prefix frontend",
    "deploy": "npm run build:frontend && gh-pages -d frontend/build"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}